name: Pull Request Workflow
on:
  - pull_request
jobs:
  run_tests:
    name: Run tests on pull requests
    runs-on: ubuntu-latest
    environment: local

    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2

      - name: Login to Quay
        uses: docker/login-action@v1
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Pull the image
        run: docker pull quay.io/1qbit/hansa:compiler.v0.2

      - name: Run tests
        run: 
           >
             docker run quay.io/1qbit/hansa:compiler.v0.2 bash -c \
          #   TODO Remove after tests have been fixed
          #   "pytest"